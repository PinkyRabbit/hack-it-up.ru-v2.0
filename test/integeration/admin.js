const chai = require('chai');

const { expect } = chai;


module.exports = () => {
  describe('Admin layout', () => {
    it('Shoult get unpublished articles on dashboard', () => {
      expect(true).to.be(true);
    });

  });
}
